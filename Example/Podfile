source 'https://github.com/CocoaPods/Specs.git'

target 'GDChannel', :exclusive => true do
  pod "GDChannel", :path => "../"
end

target 'Tests', :exclusive => true do
  pod "GDChannel", :path => "../"

  pod 'Specta'
  pod 'Expecta'
  pod 'FBSnapshotTestCase'
  pod 'Expecta+Snapshots'
end
