language: objective-c
xcode_workspace: Project/GDChannel.xcworkspace
xcode_scheme: GDChannelTests

before_install:
  - gem install cocoapods --no-rdoc --no-ri --no-document --quiet
  - cd Project && pod install
  - cd $TRAVIS_BUILD_DIR